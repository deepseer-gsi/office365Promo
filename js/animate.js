
   /** $("#main-info").on('inview', function (event, isInView) {
        if (isInView) { //если попадает обьект в злону видимости

            $("#main-info").addClass('animated-main-info zoomInDown'); //добавляем класс с animate.css
        }
    }); **/
